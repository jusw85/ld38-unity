using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Scratch : MonoBehaviour {

    //public RuntimeAnimatorController[] anims;
    //private int idx = 0;

    void Awake() {
        //string[] x = Input.GetJoystickNames();
        //Debug.Log("");
    }
    // Update is called once per frame
    void Update() {
        //if (Input.GetKeyDown(KeyCode.J)) {
        //    idx = (++idx) % anims.Length;
        //    EnemyController[] ecs = FindObjectsOfType<EnemyController>();
        //    foreach (EnemyController ec in ecs) {
        //        Animator a = ec.GetComponent<Animator>();
        //        a.runtimeAnimatorController = anims[idx];
        //    }
        //}
    }
}
